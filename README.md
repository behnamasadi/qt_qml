# Qt and Qml
This repository contains my snippets for developing qt widgets and qml application. 

```cpp
sudo apt-get -y install qml
```

```
find_package(Qt6 REQUIRED COMPONENTS Qml)
target_link_libraries(mytarget PRIVATE Qt6::Qml)
```

# QML

sudo apt install qt6-base-dev


qmlscene

